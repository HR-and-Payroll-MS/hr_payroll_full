# Backend Tools

This folder contains one-off scripts and diagnostics used during development.

## Usage

Run from the backend folder:
- Local settings: set `DJANGO_ENV=local`
- Production settings: set `DJANGO_ENV=production`

Example:

python tools/debug/check_attendance_logic.py
